# on spitfire 

# install numpy package to specific location from source
pip install numpy -t /share/lemaylab-backedup/milklab/programs/MicrobeCensus-1.1.1/scriptspip install numpy==1.14.0 -t /share/lemaylab-backedup/Zeya/scripts/ARG_metagenome/ 
# This method worked 

wget https://sourceforge.net/projects/numpy/files/NumPy/1.11.2/numpy-1.11.2.tar.gz/download
tar xfz download 
cd numpy-1.11.2/
python setup.py install
### Doesn't work because I don't have developer's header

# Add the installed directory to PATH (otherwise pip will not work)
export PATH="/home/xzyao/.local/bin:$PATH" # replace xzyao with your userid

# install Biopython package
pip install biopython --user # /afs/genomecenter.ucdavis.edu/home/xzyao/.local/lib/python2.7/site-packages 

# install cogent.parse.fastq from cogent package
pip install cogent --user

# install pandas
export PATH="/home/xzyao/miniconda3/bin:$PATH"
pip install pandas --user
### install pandas for inside the "/share/lemaylab-backedup/Zeya/scripts/ARG_metagenome"
pip install pandas -t ./